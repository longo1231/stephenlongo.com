{
  "name": "stephenlongo.com",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "sync": "node scripts/sync-notion.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@notionhq/client": "^5.1.0",
    "astro": "^5.8.0"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
